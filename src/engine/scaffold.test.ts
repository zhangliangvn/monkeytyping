import { describe, it, expect } from 'vitest'

/**
 * Smoke test: proves the Vitest harness runs. Replaced by real engine tests
 * once the spec is finalized.
 */
describe('scaffold', () => {
  it('runs the test harness', () => {
    expect(1 + 1).toBe(2)
  })
})
